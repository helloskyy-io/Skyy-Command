// proxmox.component.ts

// Import necessary Angular core module
import { Component } from '@angular/core';

// Define the ProxmoxComponent
@Component({
  selector: 'app-proxmox', // Selector for including this component in HTML
  standalone: true, // Indicates that this is a standalone component
  imports: [], // No additional imports needed for this component
  templateUrl: './proxmox.component.html', // Template for the component
  styleUrls: ['./proxmox.component.css'] // Styles for the component
})
export class ProxmoxComponent {
  // Currently, no additional logic or lifecycle hooks are needed for this component
}
